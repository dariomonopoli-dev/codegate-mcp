# CodeGate MCP

Agent-facing interface for CodeGate.

This MCP server allows autonomous agents to query CodeGate
before installing dependencies, so they can adapt behavior
instead of blindly executing blocked commands.
